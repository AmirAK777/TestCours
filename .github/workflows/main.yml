name: Industrialisation continue sur le serveur AWS (Tomcat)
on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code source
        uses: actions/checkout@v2
        with:
          path: TestCours  # Spécifiez le chemin d'installation du code source

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build et package de l'application Spring Boot
        run: |
          cd TestCours  # Accédez au répertoire du code source
          chmod +x mvnw
          ./mvnw clean package
        env:
          MAVEN_OPTS: '-Dmaven.repo.local=$GITHUB_WORKSPACE/.m2/repository'

      - name: Debug - Display file permissions
        run: ls -l TestCours/target  # Assurez-vous que le chemin est correct

      - name: Copy file via SSH
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST_DNS }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT }}
          source: "target/demo-0.0.1-SNAPSHOT.war"  # Modifié le chemin pour refléter la structure correcte
          target: "/opt/tomcat/webapps"
